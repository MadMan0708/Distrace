rootProject.name = 'distrace'

include 'core-agent'
include 'core-agent-attacher'
include 'core-instrumenter'
include 'agent-h2o'
include 'example-apps'

// Prefix all projects with project name
rootProject.children.each { it.name = rootProject.name + "-" + it.name }