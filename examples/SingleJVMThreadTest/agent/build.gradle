apply plugin: 'com.github.johnrengelman.shadow'

description = "Instrumentation agent"

dependencies {
    // use functionality provided by core-instrumentor
    compile project(path: ':distrace-core-instrumenter', configuration: 'shadow')
}

artifacts {
    archives shadowJar
}
