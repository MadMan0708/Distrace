description = "Agent build on top the agent-core designed to monitor H2O"

// Setup test environment
test {
    // Run with assertions ON
    enableAssertions = true

    // For a new JVM for each test class
    forkEvery = 1
}
